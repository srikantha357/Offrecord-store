const products = [];

for (let i = 1; i <= 34; i++) {
  products.push({
    id: i,
    name: "OVERSIZED TEE " + i,
    price: 799,
    oldPrice: 999,
    image: "assets/products/tee" + i + ".webp"
  });
}
